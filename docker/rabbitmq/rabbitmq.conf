# RabbitMQ 4.x: desactiva el recolector de métricas legacy del plugin management
# (evita el warning de deprecated_features.management_metrics_collection).
management_agent.disable_metrics_collector = true
